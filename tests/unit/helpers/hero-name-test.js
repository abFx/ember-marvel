import { heroName } from 'ember-marvel/helpers/hero-name';
import { module, test } from 'qunit';

module('Unit | Helper | hero name');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = heroName([42]);
  assert.ok(result);
});
